provider: github
branch: main
repository:
  github:
    host: github.com
    owner: testorg
    repo: testrepo
release:
  pr_title: 'chore({{ .Branch }}): release {{ .Version }}'
  commit_subject: 'chore({{ .Branch }}): release {{ .Version }}'
targets:
  default:
    type: path
    path: .
    tag_prefix: v
